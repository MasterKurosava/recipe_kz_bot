# Telegram-бот для управления рецептами (Аптека)

Бот для медицинского учреждения, который отслеживает уже выданные по рецептам лекарства, чтобы запретить повторную выдачу.

## 🚀 Возможности

- 🔍 **Проверка рецепта** - проверка, был ли рецепт уже зарегистрирован
- ➕ **Добавление рецепта** - регистрация нового выданного рецепта
- 🕓 **История рецепта** - просмотр всей истории по конкретному рецепту

## 📋 Требования

- Python 3.8+
- PostgreSQL база данных (рекомендуется Neon DB)
- Telegram Bot Token от [@BotFather](https://t.me/BotFather)

## 🔧 Установка

1. Клонируйте репозиторий или скачайте файлы

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` в корне проекта:
```
BOT_TOKEN=your_bot_token_here
DATABASE_URL=postgresql://user:password@host.neon.tech/dbname
```

4. Запустите бота:
```bash
python main.py
```

## 🗃️ База данных

Бот автоматически создаст таблицу `recipes` при первом запуске со следующей структурой:

- `id` - SERIAL PRIMARY KEY
- `recipe_id` - TEXT UNIQUE NOT NULL (уникальный ID рецепта)
- `comment` - TEXT (необязательный комментарий)
- `created_at` - TIMESTAMP DEFAULT NOW()
- `user_id` - BIGINT (Telegram user ID, который внёс рецепт)

## 📖 Использование

1. Запустите бота командой `/start`
2. Выберите нужное действие из главного меню:
   - **🔍 Проверить рецепт** - проверка наличия рецепта в базе
   - **➕ Добавить рецепт** - регистрация нового рецепта
   - **🕓 История по рецепту** - просмотр истории

## 🛠️ Структура проекта

```
apteka-bot/
├── main.py              # Точка входа, запуск бота
├── config.py            # Конфигурация и переменные окружения
├── database.py          # Работа с базой данных (asyncpg)
├── keyboards.py         # Клавиатуры для бота
├── handlers/            # Обработчики сообщений
│   ├── __init__.py
│   ├── common.py        # Общие handlers (start, menu)
│   ├── check_recipe.py  # Проверка рецепта
│   ├── add_recipe.py    # Добавление рецепта
│   └── recipe_history.py # История рецепта
├── requirements.txt     # Зависимости проекта
├── .env.example        # Пример файла с переменными окружения
└── README.md           # Документация
```

## 🔐 Безопасность

- Не публикуйте файл `.env` с реальными токенами
- Используйте переменные окружения для хранения чувствительных данных
- Обеспечьте безопасность подключения к базе данных

## 📝 Лицензия

MIT
